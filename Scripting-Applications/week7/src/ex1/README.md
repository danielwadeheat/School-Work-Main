# Week 7 — Exercise 1: Refactor & Reuse

Goal: Break big components into small ones. From Week 6, this exercise extracts `SearchBox`, `ItemRow`, and `PageTitle` components and keeps props minimal.

Run locally:

```bash
cd Scripting-Applications/week7
npm install
npm run dev
```

Open `http://localhost:5173/ex1`.
