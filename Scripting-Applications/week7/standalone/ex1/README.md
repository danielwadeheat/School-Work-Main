# Week7 Exercise 1 — Standalone

Open the `index.html` files in a browser (no Node/Vite needed) or use File → Open. This is a minimal non-React implementation of the Exercise 1 requirements: `PageTitle`, `SearchBox`, and `ItemRow` components, with a search filter and click handler.
