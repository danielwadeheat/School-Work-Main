# Week7 Exercise 3 — Standalone

Open the `index.html` files in a browser (no Node/Vite needed) or use File → Open. This page simulates the test target: it shows `Loading...` then displays items (so you can visually verify the loading -> items behavior).
