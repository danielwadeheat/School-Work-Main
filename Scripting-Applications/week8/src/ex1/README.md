# Week 8 — Exercise 1: Add/Edit Form

Goal: Create a list with add/edit form using built-in HTML validation, accessible error messages, and a simulated save.

Run locally:

```bash
cd Scripting-Applications/week8
npm install
npm run dev
```

Open `http://localhost:5173/#/ex1`.
