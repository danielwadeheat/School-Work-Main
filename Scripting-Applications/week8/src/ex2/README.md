# Week 8 — Exercise 2: Speed Touches

Goal: Add lazy-loaded images and code-split a route.

Run locally:

```bash
cd Scripting-Applications/week8
npm install
npm run dev
```

Open `http://localhost:5173/#/ex2`.
