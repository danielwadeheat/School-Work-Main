# Week 8 — Exercise 3 (Standalone)

Open with Live Server (Go Live), or open the `index.html` file in a browser (no Node/Vite needed) or use File → Open. This is a minimal non‑React implementation of the Exercise 3 requirements: instructions to build and preview a production build.

This version includes a short checklist for running `npm run build` and `npm run preview` in the Vite project.
