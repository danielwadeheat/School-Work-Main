# Week 3 – Exercise 2: Search with Cancel

This assignment demonstrates `fetch`, `AbortController`, JSON, and `async/await`.

- User types in a search box.
- A fetch starts on each input change.
- Previous request is canceled before the next one.
- Shows **Loading...**, **No results**, and friendly error states.

Open `index.html` with Live Server and type in the search box.
