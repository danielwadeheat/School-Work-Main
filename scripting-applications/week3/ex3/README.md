# Week 3 – Exercise 3: POST Demo (Fake)

This assignment demonstrates sending JSON with `fetch` POST.

- Form includes name and email.
- On submit, JSON is sent to a mock endpoint.
- Submit button is disabled while sending.
- Button is re-enabled after request finishes.
- Status message is shown for sending/success/error.

Open `index.html` with Live Server and submit the form.