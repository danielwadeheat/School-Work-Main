# Week 4 – Exercise 2: Utils Module

This assignment demonstrates splitting helper functions into a module.

- `js/utils.js` exports:
  - `$()` for DOM selection
  - `titleCase()` for string formatting
- `main.js` imports and uses both helpers

Open `index.html` with Live Server and view the output.
