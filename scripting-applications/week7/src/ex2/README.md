# Week 7 — Exercise 2: Light Context

Goal: Share a simple value via `ThemeContext` and consume it in 2+ child components.

Run locally:

```bash
cd Scripting-Applications/week7
npm install
npm run dev
```

Open `http://localhost:5173/ex2`.
