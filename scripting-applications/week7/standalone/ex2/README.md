# Week7 Exercise 2 — Standalone

Open the `index.html` files in a browser (no Node/Vite needed) or use File → Open. This is a minimal non-React implementation that demonstrates a simple Theme provider and two consumers (`PageTitle` and a paragraph) that read the theme value.
