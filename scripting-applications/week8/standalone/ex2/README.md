# Week 8 — Exercise 2 (Standalone)

Open with Live Server (Go Live), or open the `index.html` file in a browser (no Node/Vite needed) or use File → Open. This is a minimal non‑React implementation of the Exercise 2 requirements: lazy‑loaded images with explicit width/height and a small performance‑minded layout.

This version displays a small image grid that uses `loading="lazy"` and fixed dimensions for faster rendering.
